# Pb Action Portal — public app (Trade + Material Flow + Literature Stats)
streamlit>=1.44.0
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.22.0

# Interactive (draggable) Flow Network view on the Trade Relationships tab
pyvis>=0.3.2

# Literature Stats submission form -> Google Sheet (feature deferred; re-enable later)
# gspread>=6.0.0
# google-auth>=2.30.0
